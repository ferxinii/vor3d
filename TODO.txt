[ ] Every call to dt_builder_extend has to realloc the arrays? Can we improve this?
[ ] What if the nonconvex voronoi diagram did not compute the intersection between the convex voronoi diagram and the tetrahedralization of the trimesh? That is, they were kept as separate objects. Would that make everything more efficient?

[ ] build_vertex_cell_index: the idea to map each vertex to a cell containing it may be made more optimal and even reused elsewhere (in_ncell_walk for example?)
[ ] Make a context structure (EPS_DEG, TOL, randint(), rctx, ...). Even (geometry_ctx, randint(), rctx).  
    [ ] Use memory pools instead of constant malloc.
[ ] Track max error and mean error in the examples
[ ] Add a debug compilation flag to remove unnecessary checks (delaunayness etc) in the final build and only add these in debug builds ?

[ ] build weighted voronoi diagrams: center of ortho-sphere of ncells of weighted DT as voronoi vertices? Is this correct?
[ ] locally redundant points: implement function, consider when some vertices may belong to big tetra
[ ] acplx.c: replace power distance conditions with the robust versions

[ ] ncell_walk: better selection of initial random ncell?.

[ ] Mark ncells: Implement a more efficient approach. (visit_id? so no need to reset to 0s), also return a list of ncells so I dont have to run over all ncells each time?

[ ] ncvx.c Tier-B clip: the seed-vertex incident-cell star is walked twice per seed -- once in build_cell_adjacency (to build the neighbour CSR) and again in collect_voronoi_duals (to get the incident DT tets/triangles for the (a)/(c) Voronoi duals). Merge into a single pass: have build_cell_adjacency emit the Voronoi-dual lists (vertex triples / edge pairs) alongside the adjacency, so vertex_neighbors is called once per seed.
